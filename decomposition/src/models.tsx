export interface Promoblock {
    imageUrl: string;
    title: string;
    description: string;
}

export interface Exchangeblock {
    currency: string;
    rate: number;
    changeRate: string;
}

export interface Newsitem {
    icon: string;
    title: string;
    link: string;
}

